# Maximal-clique results

Results are grouped by architecture and level. Each JSON document records the
source compatible-level graph and its SHA-256 digest, the graph's ordered
`record_ids`, maximal cliques encoded as arrays of indexes into `record_ids`, and
enumeration statistics. `enumeration_complete: false` means the explicit clique
budget was reached; the stored cliques are still valid and maximal.

