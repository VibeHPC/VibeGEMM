# Ordered strategy sequences

Each maximal clique is converted into one semantic forward ordering and, when
non-trivial, one reverse alternative. Every sequence is a permutation of one
source clique and therefore remains same-level pairwise compatible. Cross-level
compatibility is deliberately unverified at this stage.
