# Progressive-refinement traces

Each architecture JSON records configuration, immutable Stage 1 source hash,
per-trajectory events, every generation/repair attempt, validation decisions,
rollback states, and the best validated backbone. Current files are marked
`placeholder_simulation`; their timings and validation outcomes are simulated.

