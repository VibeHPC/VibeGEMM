# Compatible level-graph results

This directory contains the 15 generated undirected level graphs: five levels
for each architecture. A graph is the complete unordered graph over its
`record_ids` minus the incompatible pairs from compatibility analysis. Build
with `../build.py` and validate the exact partition with `../validate.py`.
