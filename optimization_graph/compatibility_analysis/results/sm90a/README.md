# SM90a compatibility results

The five JSON files analyze every SM90a-applicable record at the corresponding
level. `record_ids` maps integer indices to stable record IDs;
`incompatible_pairs` stores `[left_index, right_index, reason_code]`, and
`reason_catalog` explains each code. The compact encoding is important for the
millions of Atom-level pairs.
