{
  "schema_version": "1.0.0",
  "id": "cutlass.sm90.atom.method.tma-load-im2col",
  "level": "atom",
  "name": "Parameterized Hopper tma load im2col method",
  "summary": "Use Hopper TMA load wrappers and im2col coordinates, selecting a supported tensor rank.",
  "primitives": [
    "TMA"
  ],
  "strategy": {
    "intent": "Map GEMM arithmetic to architecture-native Tensor Core instructions.",
    "mechanism": "Issue an asynchronous Hopper TMA load using a CUtensorMap descriptor instead of per-thread global-memory address calculations.",
    "expected_benefit": "Uses the GPU matrix-multiply datapath instead of scalar arithmetic.",
    "tradeoffs": [
      "Requires compatible fragment layouts, data types, alignment, and architecture support."
    ]
  },
  "parameters": [
    {
      "name": "tensor_rank",
      "kind": "integer",
      "description": "Supported tensor-map coordinate ranks.",
      "values": [
        3,
        4,
        5
      ]
    },
    {
      "name": "cache_hint",
      "kind": "integer",
      "description": "Optional TMA cache hint."
    }
  ],
  "constraints": [
    {
      "type": "architecture",
      "expression": "arch in {sm90, sm90a}",
      "rationale": "TMA is a Hopper data-movement facility."
    },
    {
      "type": "dependency",
      "expression": "valid_CUtensorMap_descriptor",
      "rationale": "TMA instructions consume a tensor-map descriptor."
    },
    {
      "type": "layout",
      "expression": "tensor_shape_stride_and_smem_layout_match_descriptor",
      "rationale": "Descriptor coordinates and destination layout must agree."
    },
    {
      "type": "semantic",
      "expression": "mbarrier_tracks_expected_transaction_bytes",
      "rationale": "Consumers wait for TMA load completion through an mbarrier."
    }
  ],
  "architectures": [
    "sm90",
    "sm90a"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cute/arch/copy_sm90_tma.hpp",
        "line_start": 404,
        "line_end": 404,
        "symbol": "SM90_TMA_LOAD_IM2COL_3D",
        "evidence": "CUTLASS declares this rank/operation-specific TMA wrapper at the referenced line."
      },
      {
        "path": "cutlass-main/include/cute/arch/copy_sm90_tma.hpp",
        "line_start": 455,
        "line_end": 455,
        "symbol": "SM90_TMA_LOAD_IM2COL_4D",
        "evidence": "CUTLASS declares this rank/operation-specific TMA wrapper at the referenced line."
      },
      {
        "path": "cutlass-main/include/cute/arch/copy_sm90_tma.hpp",
        "line_start": 506,
        "line_end": 506,
        "symbol": "SM90_TMA_LOAD_IM2COL_5D",
        "evidence": "CUTLASS declares this rank/operation-specific TMA wrapper at the referenced line."
      },
      {
        "path": "cutlass-main/include/cute/arch/copy_sm90_tma.hpp",
        "line_start": 557,
        "line_end": 557,
        "symbol": "SM90_TMA_LOAD_IM2COL",
        "evidence": "CUTLASS declares this rank/operation-specific TMA wrapper at the referenced line."
      }
    ]
  },
  "tags": [
    "hopper",
    "tma",
    "load",
    "im2col",
    "rank-3"
  ],
  "status": "candidate",
  "notes": [
    "Automatically normalized from concrete CUTLASS wrapper candidates.",
    "All concrete candidate IDs are retained in atom-method-membership.json.",
    "Requires semantic review before promotion to curated."
  ]
}
