{
  "schema_version": "1.0.0",
  "id": "cutlass.sm90.tile.cluster-to-tma-multicast",
  "level": "tile",
  "name": "Derive TMA multicast atoms from cluster shape",
  "summary": "Choose ordinary or multicast TMA copy atoms for A and B from the orthogonal dimensions of the Hopper CTA cluster shape.",
  "primitives": [
    "sm90_cluster_shape_to_tma_atom"
  ],
  "strategy": {
    "intent": "Construct a legal and performance-relevant GEMM tile mapping for the target architecture.",
    "mechanism": "Map cluster N extent to A movement and cluster M extent to B movement so peer CTAs can reuse operand tiles.",
    "expected_benefit": "Exposes data reuse and parallel Tensor Core work while respecting layout and resource limits.",
    "tradeoffs": [
      "Tile choices change shared-memory use, register pressure, occupancy, and available grid parallelism."
    ]
  },
  "parameters": [
    {
      "name": "cluster_shape_mnk",
      "kind": "shape",
      "description": "Tunable or derived tile property: cluster_shape_mnk."
    },
    {
      "name": "multicast_a",
      "kind": "boolean",
      "description": "Tunable or derived tile property: multicast_a."
    },
    {
      "name": "multicast_b",
      "kind": "boolean",
      "description": "Tunable or derived tile property: multicast_b."
    }
  ],
  "constraints": [
    {
      "type": "dependency",
      "expression": "TMA multicast Atom method available when cluster extent > 1",
      "rationale": "Cross-CTA reuse requires multicast-capable TMA loads."
    },
    {
      "type": "shape",
      "expression": "cluster_k == 1 for standard dense GEMM schedules",
      "rationale": "Most Hopper GEMM cluster reuse is organized over M and N."
    }
  ],
  "architectures": [
    "sm90",
    "sm90a"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/gemm/collective/builders/sm90_gmma_builder.inl",
        "line_start": 261,
        "line_end": 261,
        "symbol": "sm90_cluster_shape_to_tma_atom",
        "evidence": "The referenced CUTLASS builder/core expression implements this tile decision."
      }
    ]
  },
  "tags": [
    "hopper",
    "cluster",
    "tma",
    "multicast",
    "operand-reuse"
  ],
  "status": "candidate",
  "notes": [
    "Rule-extracted parameterized method; requires semantic review before promotion to curated."
  ]
}
