{
  "schema_version": "1.0.0",
  "id": "cutlass.device.hardware-info",
  "level": "device",
  "name": "Kernel hardware information",
  "summary": "Carry device ID, SM count, and preferred/fallback cluster shapes into kernel parameter lowering and scheduling.",
  "primitives": [
    "struct KernelHardwareInfo"
  ],
  "strategy": {
    "intent": "Turn a composed GEMM kernel into a safe, reusable host-callable operation.",
    "mechanism": "Carry device ID, SM count, and preferred/fallback cluster shapes into kernel parameter lowering and scheduling.",
    "expected_benefit": "Provides uniform validation, resource management, launch, and workload-mode integration.",
    "tradeoffs": [
      "Device wrappers add host-side policy and workspace requirements that must remain consistent with the selected kernel."
    ]
  },
  "parameters": [
    {
      "name": "device_id",
      "kind": "choice",
      "description": "Device integration parameter: device_id."
    },
    {
      "name": "sm_count",
      "kind": "integer",
      "description": "Device integration parameter: sm_count."
    },
    {
      "name": "cluster_shape",
      "kind": "choice",
      "description": "Device integration parameter: cluster_shape."
    }
  ],
  "constraints": [
    {
      "type": "resource",
      "expression": "hardware information describes the launch device",
      "rationale": "Required for a valid and correctly ordered device execution."
    }
  ],
  "architectures": [
    "sm90",
    "sm90a"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/kernel_hardware_info.h",
        "line_start": 43,
        "line_end": 43,
        "symbol": "struct KernelHardwareInfo",
        "evidence": "The referenced CUTLASS device API implements this integration method."
      }
    ]
  },
  "tags": [
    "device-api",
    "hardware-info",
    "scheduler"
  ],
  "status": "candidate",
  "notes": [
    "Rule-extracted method; requires semantic review before promotion to curated."
  ]
}
