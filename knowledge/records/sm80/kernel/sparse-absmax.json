{
  "schema_version": "1.0.0",
  "id": "cutlass.sm80.kernel.sparse-absmax",
  "level": "kernel",
  "name": "Sparse GEMM AbsMax kernel",
  "summary": "Compose structured-sparse GEMM with converted output and fused absolute-maximum statistics.",
  "primitives": [
    "struct SparseGemmWithAbsmax"
  ],
  "strategy": {
    "intent": "Compose validated GEMM collectives with a grid/work scheduler into a complete CUDA kernel.",
    "mechanism": "Compose structured-sparse GEMM with converted output and fused absolute-maximum statistics.",
    "expected_benefit": "Improves device utilization, overlap, locality, or workload coverage while preserving collective implementations.",
    "tradeoffs": [
      "Kernel schedules constrain tile shapes, resources, synchronization, and applicable problem regimes."
    ]
  },
  "parameters": [
    {
      "name": "metadata",
      "kind": "choice",
      "description": "Kernel configuration parameter: metadata."
    },
    {
      "name": "absmax_output",
      "kind": "choice",
      "description": "Kernel configuration parameter: absmax_output."
    }
  ],
  "constraints": [
    {
      "type": "semantic",
      "expression": "absmax covers every final output",
      "rationale": "Required for correctness or feasibility of the composed kernel schedule."
    }
  ],
  "architectures": [
    "sm80"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/gemm/kernel/sparse_gemm_with_absmax.h",
        "line_start": 60,
        "line_end": 60,
        "symbol": "struct SparseGemmWithAbsmax",
        "evidence": "The referenced CUTLASS kernel or scheduler implements this composition strategy."
      }
    ]
  },
  "tags": [
    "ampere",
    "sparse",
    "absmax",
    "quantization"
  ],
  "status": "candidate",
  "notes": [
    "Rule-extracted method; requires semantic review before promotion to curated."
  ]
}
