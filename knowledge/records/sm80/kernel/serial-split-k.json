{
  "schema_version": "1.0.0",
  "id": "cutlass.sm80.kernel.serial-split-k",
  "level": "kernel",
  "name": "SM80 serial split-K kernel",
  "summary": "Partition K while serializing output-tile accumulation through workspace semaphores rather than a separate reduction kernel.",
  "primitives": [
    "bool SplitKSerial"
  ],
  "strategy": {
    "intent": "Compose validated GEMM collectives with a grid/work scheduler into a complete CUDA kernel.",
    "mechanism": "Partition K while serializing output-tile accumulation through workspace semaphores rather than a separate reduction kernel.",
    "expected_benefit": "Improves device utilization, overlap, locality, or workload coverage while preserving collective implementations.",
    "tradeoffs": [
      "Kernel schedules constrain tile shapes, resources, synchronization, and applicable problem regimes."
    ]
  },
  "parameters": [
    {
      "name": "split_k_slices",
      "kind": "integer",
      "description": "Kernel configuration parameter: split_k_slices."
    },
    {
      "name": "semaphore_workspace",
      "kind": "choice",
      "description": "Kernel configuration parameter: semaphore_workspace."
    }
  ],
  "constraints": [
    {
      "type": "semantic",
      "expression": "partitions acquire output tile in K order",
      "rationale": "Required for correctness or feasibility of the composed kernel schedule."
    }
  ],
  "architectures": [
    "sm80"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/gemm/kernel/default_gemm.h",
        "line_start": 119,
        "line_end": 119,
        "symbol": "bool SplitKSerial",
        "evidence": "The referenced CUTLASS kernel or scheduler implements this composition strategy."
      }
    ]
  },
  "tags": [
    "ampere",
    "split-k",
    "serial",
    "semaphore"
  ],
  "status": "candidate",
  "notes": [
    "Rule-extracted method; requires semantic review before promotion to curated."
  ]
}
