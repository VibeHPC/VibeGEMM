{
  "schema_version": "1.0.0",
  "id": "cutlass.sm80.tile.shared-memory-tile-iterators",
  "level": "tile",
  "name": "SM80 regular shared-memory tile iterators",
  "summary": "Map cooperating CTA threads onto regular shared-memory A and B tiles consumed by warp-level Tensor Core MMA.",
  "primitives": [
    "RegularTileAccessIterator<"
  ],
  "strategy": {
    "intent": "Construct a legal and performance-relevant GEMM tile mapping for the target architecture.",
    "mechanism": "Construct operand-specific shared-memory iterators from tile shapes, layouts, thread maps, and vector access widths.",
    "expected_benefit": "Exposes data reuse and parallel Tensor Core work while respecting layout and resource limits.",
    "tradeoffs": [
      "Tile choices change shared-memory use, register pressure, occupancy, and available grid parallelism."
    ]
  },
  "parameters": [
    {
      "name": "smem_layout_a",
      "kind": "choice",
      "description": "Tunable or derived tile property: smem_layout_a."
    },
    {
      "name": "smem_layout_b",
      "kind": "choice",
      "description": "Tunable or derived tile property: smem_layout_b."
    },
    {
      "name": "elements_per_access",
      "kind": "integer",
      "description": "Tunable or derived tile property: elements_per_access."
    }
  ],
  "constraints": [
    {
      "type": "layout",
      "expression": "iterator_layout matches warp_mma_operand_layout",
      "rationale": "Warp fragment loads must agree with shared-memory storage."
    },
    {
      "type": "resource",
      "expression": "vector_access_is_aligned",
      "rationale": "Vectorized shared-memory accesses require matching alignment."
    }
  ],
  "architectures": [
    "sm80"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/gemm/threadblock/default_mma_core_sm80.h",
        "line_start": 164,
        "line_end": 164,
        "symbol": "RegularTileAccessIterator<",
        "evidence": "The referenced CUTLASS builder/core expression implements this tile decision."
      }
    ]
  },
  "tags": [
    "ampere",
    "shared-memory",
    "tile-iterator",
    "vectorized-access"
  ],
  "status": "candidate",
  "notes": [
    "Rule-extracted parameterized method; requires semantic review before promotion to curated."
  ]
}
