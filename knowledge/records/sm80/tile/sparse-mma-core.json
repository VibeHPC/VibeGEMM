{
  "schema_version": "1.0.0",
  "id": "cutlass.sm80.tile.sparse-mma-core",
  "level": "tile",
  "name": "SM80 sparse MMA tile core",
  "summary": "Construct CTA and warp tiles for structured-sparse Tensor Core MMA, including compressed operand and metadata tile mappings.",
  "primitives": [
    "struct DefaultSparseMmaCore<"
  ],
  "strategy": {
    "intent": "Construct a legal and performance-relevant GEMM tile mapping for the target architecture.",
    "mechanism": "Bind sparse instruction shape, warp shape, operand layouts, and metadata iterators into a sparse threadblock core.",
    "expected_benefit": "Exposes data reuse and parallel Tensor Core work while respecting layout and resource limits.",
    "tradeoffs": [
      "Tile choices change shared-memory use, register pressure, occupancy, and available grid parallelism."
    ]
  },
  "parameters": [
    {
      "name": "threadblock_shape",
      "kind": "shape",
      "description": "Tunable or derived tile property: threadblock_shape."
    },
    {
      "name": "warp_shape",
      "kind": "shape",
      "description": "Tunable or derived tile property: warp_shape."
    },
    {
      "name": "metadata_layout",
      "kind": "choice",
      "description": "Tunable or derived tile property: metadata_layout."
    }
  ],
  "constraints": [
    {
      "type": "dependency",
      "expression": "sparse MMA atom supports instruction shape",
      "rationale": "The tile core ultimately issues the selected sparse atom."
    },
    {
      "type": "layout",
      "expression": "metadata layout matches compressed operand",
      "rationale": "Sparse MMA consumes encoded nonzero positions."
    }
  ],
  "architectures": [
    "sm80"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/gemm/threadblock/default_mma_core_sparse_sm80.h",
        "line_start": 153,
        "line_end": 153,
        "symbol": "struct DefaultSparseMmaCore<",
        "evidence": "The referenced CUTLASS builder/core expression implements this tile decision."
      }
    ]
  },
  "tags": [
    "ampere",
    "sparse",
    "tile-core",
    "metadata"
  ],
  "status": "candidate",
  "notes": [
    "Rule-extracted parameterized method; requires semantic review before promotion to curated."
  ]
}
