{
  "schema_version": "1.0.0",
  "id": "cutlass.sm80.tile.warp-tensorop-policy",
  "level": "tile",
  "name": "SM80 warp TensorOp tile policy",
  "summary": "Select the warp-level TensorOp implementation that maps a warp tile onto compatible SM80 MMA atoms and shared-memory layouts.",
  "primitives": [
    "DefaultMmaTensorOp<"
  ],
  "strategy": {
    "intent": "Construct a legal and performance-relevant GEMM tile mapping for the target architecture.",
    "mechanism": "Instantiate DefaultMmaTensorOp from warp shape, instruction shape, operand types/layouts, accumulator type, and K partitions.",
    "expected_benefit": "Exposes data reuse and parallel Tensor Core work while respecting layout and resource limits.",
    "tradeoffs": [
      "Tile choices change shared-memory use, register pressure, occupancy, and available grid parallelism."
    ]
  },
  "parameters": [
    {
      "name": "warp_shape",
      "kind": "shape",
      "description": "Tunable or derived tile property: warp_shape."
    },
    {
      "name": "instruction_shape",
      "kind": "shape",
      "description": "Tunable or derived tile property: instruction_shape."
    },
    {
      "name": "partitions_k",
      "kind": "integer",
      "description": "Tunable or derived tile property: partitions_k."
    }
  ],
  "constraints": [
    {
      "type": "dependency",
      "expression": "instruction_shape supported by selected SM80 MMA atom",
      "rationale": "The warp policy ultimately issues the architecture atom."
    },
    {
      "type": "shape",
      "expression": "warp_k % instruction_k == 0",
      "rationale": "The warp K tile must be expressible as MMA iterations."
    }
  ],
  "architectures": [
    "sm80"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/gemm/threadblock/default_mma_core_sm80.h",
        "line_start": 188,
        "line_end": 188,
        "symbol": "DefaultMmaTensorOp<",
        "evidence": "The referenced CUTLASS builder/core expression implements this tile decision."
      }
    ]
  },
  "tags": [
    "ampere",
    "warp-tile",
    "tensorop",
    "mma-policy"
  ],
  "status": "candidate",
  "notes": [
    "Rule-extracted parameterized method; requires semantic review before promotion to curated."
  ]
}
