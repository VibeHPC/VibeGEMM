{
  "schema_version": "1.0.0",
  "id": "cutlass.device.update-without-rebuild",
  "level": "device",
  "name": "Update reusable GEMM operator",
  "summary": "Replace launch parameters while reusing an already constructed device operator and compatible workspace.",
  "primitives": [
    "update(Arguments const& args, void* workspace"
  ],
  "strategy": {
    "intent": "Turn a composed GEMM kernel into a safe, reusable host-callable operation.",
    "mechanism": "Replace launch parameters while reusing an already constructed device operator and compatible workspace.",
    "expected_benefit": "Provides uniform validation, resource management, launch, and workload-mode integration.",
    "tradeoffs": [
      "Device wrappers add host-side policy and workspace requirements that must remain consistent with the selected kernel."
    ]
  },
  "parameters": [
    {
      "name": "arguments",
      "kind": "choice",
      "description": "Device integration parameter: arguments."
    },
    {
      "name": "workspace",
      "kind": "choice",
      "description": "Device integration parameter: workspace."
    }
  ],
  "constraints": [
    {
      "type": "resource",
      "expression": "updated arguments fit the supplied workspace",
      "rationale": "Required for a valid and correctly ordered device execution."
    }
  ],
  "architectures": [
    "sm80",
    "sm90",
    "sm90a"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/gemm/device/gemm_universal_adapter.h",
        "line_start": 360,
        "line_end": 360,
        "symbol": "update(Arguments const& args, void* workspace",
        "evidence": "The referenced CUTLASS device API implements this integration method."
      }
    ]
  },
  "tags": [
    "device-api",
    "update",
    "reuse"
  ],
  "status": "candidate",
  "notes": [
    "Rule-extracted method; requires semantic review before promotion to curated."
  ]
}
