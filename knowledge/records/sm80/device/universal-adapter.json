{
  "schema_version": "1.0.0",
  "id": "cutlass.device.universal-adapter",
  "level": "device",
  "name": "Universal GEMM device adapter",
  "summary": "Expose a kernel through a stable host API for feasibility checks, workspace management, initialization, update, and stream launch.",
  "primitives": [
    "class GemmUniversalAdapter<"
  ],
  "strategy": {
    "intent": "Turn a composed GEMM kernel into a safe, reusable host-callable operation.",
    "mechanism": "Expose a kernel through a stable host API for feasibility checks, workspace management, initialization, update, and stream launch.",
    "expected_benefit": "Provides uniform validation, resource management, launch, and workload-mode integration.",
    "tradeoffs": [
      "Device wrappers add host-side policy and workspace requirements that must remain consistent with the selected kernel."
    ]
  },
  "parameters": [
    {
      "name": "arguments",
      "kind": "choice",
      "description": "Device integration parameter: arguments."
    },
    {
      "name": "workspace",
      "kind": "choice",
      "description": "Device integration parameter: workspace."
    },
    {
      "name": "stream",
      "kind": "choice",
      "description": "Device integration parameter: stream."
    }
  ],
  "constraints": [
    {
      "type": "dependency",
      "expression": "GemmKernel supplies the adapter contract",
      "rationale": "Required for a valid and correctly ordered device execution."
    }
  ],
  "architectures": [
    "sm80",
    "sm90",
    "sm90a"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/gemm/device/gemm_universal_adapter.h",
        "line_start": 123,
        "line_end": 123,
        "symbol": "class GemmUniversalAdapter<",
        "evidence": "The referenced CUTLASS device API implements this integration method."
      }
    ]
  },
  "tags": [
    "device-api",
    "universal-gemm",
    "adapter"
  ],
  "status": "candidate",
  "notes": [
    "Rule-extracted method; requires semantic review before promotion to curated."
  ]
}
