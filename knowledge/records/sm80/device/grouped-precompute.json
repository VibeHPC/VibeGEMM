{
  "schema_version": "1.0.0",
  "id": "cutlass.device.grouped-precompute",
  "level": "device",
  "name": "Grouped schedule precomputation",
  "summary": "Precompute grouped problem-to-tile metadata on the host and asynchronously copy it into device workspace.",
  "primitives": [
    "Status precompute"
  ],
  "strategy": {
    "intent": "Turn a composed GEMM kernel into a safe, reusable host-callable operation.",
    "mechanism": "Precompute grouped problem-to-tile metadata on the host and asynchronously copy it into device workspace.",
    "expected_benefit": "Provides uniform validation, resource management, launch, and workload-mode integration.",
    "tradeoffs": [
      "Device wrappers add host-side policy and workspace requirements that must remain consistent with the selected kernel."
    ]
  },
  "parameters": [
    {
      "name": "tile_count",
      "kind": "integer",
      "description": "Device integration parameter: tile_count."
    },
    {
      "name": "workspace",
      "kind": "choice",
      "description": "Device integration parameter: workspace."
    },
    {
      "name": "stream",
      "kind": "choice",
      "description": "Device integration parameter: stream."
    }
  ],
  "constraints": [
    {
      "type": "resource",
      "expression": "workspace holds the precomputed schedule",
      "rationale": "Required for a valid and correctly ordered device execution."
    }
  ],
  "architectures": [
    "sm80",
    "sm90",
    "sm90a"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/gemm/device/base_grouped.h",
        "line_start": 145,
        "line_end": 145,
        "symbol": "Status precompute",
        "evidence": "The referenced CUTLASS device API implements this integration method."
      }
    ]
  },
  "tags": [
    "device-api",
    "grouped-gemm",
    "precompute",
    "workspace"
  ],
  "status": "candidate",
  "notes": [
    "Rule-extracted method; requires semantic review before promotion to curated."
  ]
}
