{
  "schema_version": "1.0.0",
  "id": "cutlass.device.workspace-protocol",
  "level": "device",
  "name": "Workspace sizing and initialization",
  "summary": "Compute scheduler and kernel workspace requirements, initialize that storage on the target stream, and bind it into kernel parameters.",
  "primitives": [
    "get_workspace_size(Arguments const& args)"
  ],
  "strategy": {
    "intent": "Turn a composed GEMM kernel into a safe, reusable host-callable operation.",
    "mechanism": "Compute scheduler and kernel workspace requirements, initialize that storage on the target stream, and bind it into kernel parameters.",
    "expected_benefit": "Provides uniform validation, resource management, launch, and workload-mode integration.",
    "tradeoffs": [
      "Device wrappers add host-side policy and workspace requirements that must remain consistent with the selected kernel."
    ]
  },
  "parameters": [
    {
      "name": "arguments",
      "kind": "choice",
      "description": "Device integration parameter: arguments."
    },
    {
      "name": "workspace_bytes",
      "kind": "integer",
      "description": "Device integration parameter: workspace_bytes."
    },
    {
      "name": "stream",
      "kind": "choice",
      "description": "Device integration parameter: stream."
    }
  ],
  "constraints": [
    {
      "type": "resource",
      "expression": "workspace is non-null when required",
      "rationale": "Required for a valid and correctly ordered device execution."
    },
    {
      "type": "resource",
      "expression": "allocated bytes >= get_workspace_size(args)",
      "rationale": "Required for a valid and correctly ordered device execution."
    }
  ],
  "architectures": [
    "sm80",
    "sm90",
    "sm90a"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/gemm/device/gemm_universal_adapter.h",
        "line_start": 243,
        "line_end": 243,
        "symbol": "get_workspace_size(Arguments const& args)",
        "evidence": "The referenced CUTLASS device API implements this integration method."
      }
    ]
  },
  "tags": [
    "device-api",
    "workspace",
    "initialization"
  ],
  "status": "candidate",
  "notes": [
    "Rule-extracted method; requires semantic review before promotion to curated."
  ]
}
