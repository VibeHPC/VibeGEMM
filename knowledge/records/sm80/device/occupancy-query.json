{
  "schema_version": "1.0.0",
  "id": "cutlass.device.occupancy-query",
  "level": "device",
  "name": "Kernel occupancy query",
  "summary": "Set the required dynamic shared-memory attribute and query the maximum active blocks per SM for launch planning.",
  "primitives": [
    "maximum_active_blocks"
  ],
  "strategy": {
    "intent": "Turn a composed GEMM kernel into a safe, reusable host-callable operation.",
    "mechanism": "Set the required dynamic shared-memory attribute and query the maximum active blocks per SM for launch planning.",
    "expected_benefit": "Provides uniform validation, resource management, launch, and workload-mode integration.",
    "tradeoffs": [
      "Device wrappers add host-side policy and workspace requirements that must remain consistent with the selected kernel."
    ]
  },
  "parameters": [
    {
      "name": "smem_capacity",
      "kind": "integer",
      "description": "Device integration parameter: smem_capacity."
    }
  ],
  "constraints": [
    {
      "type": "resource",
      "expression": "requested shared memory does not exceed the device opt-in limit",
      "rationale": "Required for a valid and correctly ordered device execution."
    }
  ],
  "architectures": [
    "sm80",
    "sm90",
    "sm90a"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/gemm/device/gemm_universal_adapter.h",
        "line_start": 270,
        "line_end": 270,
        "symbol": "maximum_active_blocks",
        "evidence": "The referenced CUTLASS device API implements this integration method."
      }
    ]
  },
  "tags": [
    "device-api",
    "occupancy",
    "shared-memory"
  ],
  "status": "candidate",
  "notes": [
    "Rule-extracted method; requires semantic review before promotion to curated."
  ]
}
