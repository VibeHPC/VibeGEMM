{
  "schema_version": "1.0.0",
  "id": "cutlass.device.launch-error-check",
  "level": "device",
  "name": "CUDA launch error detection",
  "summary": "Check CUDA runtime launch status immediately and translate failures into the CUTLASS status protocol.",
  "primitives": [
    "cudaGetLastError()"
  ],
  "strategy": {
    "intent": "Turn a composed GEMM kernel into a safe, reusable host-callable operation.",
    "mechanism": "Check CUDA runtime launch status immediately and translate failures into the CUTLASS status protocol.",
    "expected_benefit": "Provides uniform validation, resource management, launch, and workload-mode integration.",
    "tradeoffs": [
      "Device wrappers add host-side policy and workspace requirements that must remain consistent with the selected kernel."
    ]
  },
  "parameters": [
    {
      "name": "cuda_error",
      "kind": "choice",
      "description": "Device integration parameter: cuda_error."
    }
  ],
  "constraints": [
    {
      "type": "semantic",
      "expression": "launch failure prevents accepting the candidate as valid",
      "rationale": "Required for a valid and correctly ordered device execution."
    }
  ],
  "architectures": [
    "sm80",
    "sm90",
    "sm90a"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/gemm/device/gemm_universal_adapter.h",
        "line_start": 284,
        "line_end": 284,
        "symbol": "cudaGetLastError()",
        "evidence": "The referenced CUTLASS device API implements this integration method."
      }
    ]
  },
  "tags": [
    "device-api",
    "error-check",
    "validation",
    "rollback"
  ],
  "status": "candidate",
  "notes": [
    "Rule-extracted method; requires semantic review before promotion to curated."
  ]
}
