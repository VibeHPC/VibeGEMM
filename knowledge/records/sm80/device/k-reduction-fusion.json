{
  "schema_version": "1.0.0",
  "id": "cutlass.device.k-reduction-fusion",
  "level": "device",
  "name": "GEMM with fused K reduction",
  "summary": "Manage arguments, workspace, and launch for GEMM with an auxiliary K-related reduction output.",
  "primitives": [
    "class GemmWithKReduction"
  ],
  "strategy": {
    "intent": "Turn a composed GEMM kernel into a safe, reusable host-callable operation.",
    "mechanism": "Manage arguments, workspace, and launch for GEMM with an auxiliary K-related reduction output.",
    "expected_benefit": "Provides uniform validation, resource management, launch, and workload-mode integration.",
    "tradeoffs": [
      "Device wrappers add host-side policy and workspace requirements that must remain consistent with the selected kernel."
    ]
  },
  "parameters": [
    {
      "name": "reduction_output",
      "kind": "choice",
      "description": "Device integration parameter: reduction_output."
    },
    {
      "name": "workspace",
      "kind": "choice",
      "description": "Device integration parameter: workspace."
    },
    {
      "name": "stream",
      "kind": "choice",
      "description": "Device integration parameter: stream."
    }
  ],
  "constraints": [
    {
      "type": "semantic",
      "expression": "reduction buffer has the required extent",
      "rationale": "Required for a valid and correctly ordered device execution."
    }
  ],
  "architectures": [
    "sm80"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/gemm/device/gemm_with_k_reduction.h",
        "line_start": 135,
        "line_end": 135,
        "symbol": "class GemmWithKReduction",
        "evidence": "The referenced CUTLASS device API implements this integration method."
      }
    ]
  },
  "tags": [
    "device-api",
    "reduction",
    "fusion"
  ],
  "status": "candidate",
  "notes": [
    "Rule-extracted method; requires semantic review before promotion to curated."
  ]
}
