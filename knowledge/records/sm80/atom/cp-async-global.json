{
  "schema_version": "1.0.0",
  "id": "cutlass.sm80.atom.cp-async-global",
  "level": "atom",
  "name": "SM80 cp_async with Global cache policy",
  "summary": "Use the Ampere cp_async wrapper to copy a global-memory access into shared memory asynchronously under the Global cache policy.",
  "primitives": [
    "cp.async",
    "cp_async",
    "CacheOperation::Global"
  ],
  "strategy": {
    "intent": "Map GEMM arithmetic to architecture-native Tensor Core instructions.",
    "mechanism": "Issue cp.async from global to shared memory and use the wrapper's predicated copy behavior.",
    "expected_benefit": "Uses the GPU matrix-multiply datapath instead of scalar arithmetic.",
    "tradeoffs": [
      "Requires compatible fragment layouts, data types, alignment, and architecture support."
    ]
  },
  "parameters": [
    {
      "name": "access_bytes",
      "kind": "integer",
      "description": "Asynchronous copy access width supported by the specialization."
    },
    {
      "name": "cache_policy",
      "kind": "choice",
      "description": "L1/L2 cache behavior.",
      "values": [
        "Global"
      ]
    }
  ],
  "constraints": [
    {
      "type": "architecture",
      "expression": "arch >= sm80",
      "rationale": "cp.async first appears on Ampere."
    },
    {
      "type": "layout",
      "expression": "global_and_shared_addresses_satisfy_access_alignment",
      "rationale": "The instruction requires alignment matching its access width."
    },
    {
      "type": "dependency",
      "expression": "destination_in_shared_memory",
      "rationale": "cp.async transfers from global memory to shared memory."
    }
  ],
  "architectures": [
    "sm80"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/arch/memory_sm80.h",
        "line_start": 319,
        "line_end": 319,
        "symbol": "cp_async",
        "evidence": "CUTLASS specializes this cp.async wrapper and cache policy at the referenced declaration."
      }
    ]
  },
  "tags": [
    "ampere",
    "cp-async",
    "global",
    "global-to-shared"
  ],
  "status": "candidate",
  "notes": [
    "Automatically extracted; requires semantic review before promotion to curated."
  ]
}
