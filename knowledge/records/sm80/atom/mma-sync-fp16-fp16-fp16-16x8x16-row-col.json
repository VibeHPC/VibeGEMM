{
  "schema_version": "1.0.0",
  "id": "cutlass.sm80.atom.mma-sync-fp16-fp16-fp16-16x8x16-row-col",
  "level": "atom",
  "name": "SM80 MMA.SYNC 16x8x16 fp16/fp16 to fp16",
  "summary": "Issue an Ampere warp-level 16x8x16 MMA.SYNC operation with fp16 and fp16 inputs, fp16 source accumulators, and fp16 outputs.",
  "primitives": [
    "mma.sync",
    "mma.sync.aligned.m16n8k16.row.col.f16.f16.f16.f16"
  ],
  "strategy": {
    "intent": "Map GEMM arithmetic to architecture-native Tensor Core instructions.",
    "mechanism": "Distribute register fragments across 32 threads and issue the exact MMA.SYNC PTX instruction exposed by the CUTLASS wrapper.",
    "expected_benefit": "Uses the GPU matrix-multiply datapath instead of scalar arithmetic.",
    "tradeoffs": [
      "Requires compatible fragment layouts, data types, alignment, and architecture support."
    ]
  },
  "parameters": [
    {
      "name": "layout_a",
      "kind": "choice",
      "description": "Instruction layout for A.",
      "values": [
        "row"
      ]
    },
    {
      "name": "layout_b",
      "kind": "choice",
      "description": "Instruction layout for B.",
      "values": [
        "col"
      ]
    }
  ],
  "constraints": [
    {
      "type": "architecture",
      "expression": "arch >= sm80",
      "rationale": "The instruction is exposed by the SM80 architecture wrapper."
    },
    {
      "type": "dtype",
      "expression": "A == fp16 && B == fp16 && C == fp16 && D == fp16",
      "rationale": "Operand types are encoded by the PTX instruction."
    },
    {
      "type": "shape",
      "expression": "mma_shape == (16, 8, 16)",
      "rationale": "Instruction shape is fixed in PTX."
    },
    {
      "type": "dependency",
      "expression": "threads_per_mma == 32",
      "rationale": "MMA.SYNC is warp-level."
    }
  ],
  "architectures": [
    "sm80"
  ],
  "source": {
    "library": "CUTLASS",
    "version": "4.7.0",
    "license": "BSD-3-Clause",
    "references": [
      {
        "path": "cutlass-main/include/cutlass/arch/mma_sm80.h",
        "line_start": 317,
        "line_end": 317,
        "symbol": "mma.sync.aligned",
        "evidence": "The CUTLASS wrapper emits this exact SM80 MMA.SYNC instruction."
      }
    ]
  },
  "tags": [
    "ampere",
    "mma-sync",
    "tensor-core",
    "fp16"
  ],
  "status": "candidate",
  "notes": [
    "Automatically extracted; requires semantic review before promotion to curated."
  ]
}
