# Patch-localization results

Results are grouped by target architecture. Each variant document is content-
addressed to its analysis, refinement trace, and best backbone. Current Stage 2
backbones are placeholders, so localization status is `symbolic_localized`.

