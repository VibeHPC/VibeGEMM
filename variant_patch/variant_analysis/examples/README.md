# Variant-analysis inputs

`representative_variants.json` contains the six GEMM-variant families evaluated
in the paper. An input declares tensors, a directed operation graph, target
outputs, supported architectures, problem-shape examples, and reference metadata.
The operation graph is semantic input; this stage does not execute the reference.

