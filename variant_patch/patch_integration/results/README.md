# Patch-integration results

Results are grouped by architecture and variant. Each document stores immutable
localization/backbone hashes, one prompt and source transition per localized group,
the atomically committed placeholder candidate, and an explicit rollback target.

